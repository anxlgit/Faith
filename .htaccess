RewriteEngine On
RewriteCond %{HTTP_HOST} anxl\.faith [NC]
RewriteCond %{SERVER_PORT} 80
RewriteRule ^(.*)$ https://anxl.faith/$1 [R,L]

ErrorDocument 404 /nofound.html

RewriteEngine on
RewriteCond %{HTTPS} !=on [NC]
RewriteRule ^.*$ https://%{SERVER_NAME}%{REQUEST_URI} [R,L]
